#!/bin/bash

reinstall_modules() {
  DIR=$1

  pushd $DIR
  rm -rf node_modules/
  npm install
  popd
}

npm cache clean --force 2> /dev/null

reinstall_modules functions
reinstall_modules web

npm cache verify
